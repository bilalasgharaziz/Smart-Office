import React from "react";

// NotFound component is a functional component used to display a message when a page is not found.
const NotFound = () => {
  return (
    <div>
      {/* Displaying a message indicating that the requested page is not found */}
      <h1>Opps! We ran out of code</h1>
    </div>
  );
};

export default NotFound;
