import React from "react";
import Routes from "./Routes";

// App component is the root component of the application.
// It serves as the main entry point for the application's structure and routing.

function App() {
  // The return statement renders the Routes component.
  // Routes component is responsible for handling the routing logic of the application.
  return <Routes />;
}

export default App; // Exporting App component for use in index.js (or the equivalent entry file).
